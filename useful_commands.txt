sudo docker network create -d bridge my_bridge_network
sudo docker images
sudo docker ps
sudo docker ps -a
sudo docker exrc -it  <container_id> bash 
psql -U artemy englishtrainer_db

curl -s https://packagecloud.io/install/repositories/golang-migrate/migrate/script.deb.sh | sudo bash
sudo apt-get update
sudo apt-get install migrate

docker-compose --env-file .my-env  up -d